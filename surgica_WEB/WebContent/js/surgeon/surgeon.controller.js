angular.module('hb.smartcard.controller.Surgeon', [])

.controller("SurgeonManager", [ "$rootScope", "$scope", "$filter", "$firebase", "$log", "$location", "SurgeonFactory", function($rootScope, $scope, $filter, $firebase, $log, $location, SurgeonFactory) {
	$scope.isloaded = false;
	
}]);